# Router Contract

## Contract Goal

Keep Forge routing predictable while preserving Token Saver defaults and silent activation.

## Route

- `id`:
- `name`:
- `priority`:
- `triggers`:
- `minimum_reference`:
- `optional_references`:
- `default_output`:
- `completion_evidence`:
- `handoff`:
- `token_mode_default`:

## Prompt Simulation

| Prompt | Expected Route | Evidence |
|---|---|---|
| | | |

## Boundary Notes

- Small tasks stay Lite.
- Load one minimum reference by default.
- Chain routes only when it changes the next action or reduces risk.
- Burn Mode changes output expansion only, not the core recommendation.
- Completion claims require verification evidence or an explicit unverified note.
